# 古韵抽卡 · 扩展与加固规划（v2.1）

> 目标：在已修复 429 的基础上，进一步「严格控制请求频率 / 失败显式提示与降级 / 美化 UI / 规划扩展功能」。

## 进度（实时）
- ✅ **阶段 1（核心诉求，已完成）**：令牌桶 `rate-limit.js` + 熔断 `circuit-breaker.js` + 统一请求层 `api.js` + `main.js` 接入 + 骨架屏 + 降级横幅。
  - 经 Node ESM 自测：令牌桶并发上限/令牌门控、429→限流分类、网络→network、500/404→http、熔断开路后抑制多余请求，全部通过。
  - 🐛 **发现并修复令牌桶死锁**：原实现只在 `acquire`/`release` 时 `_pump`，空桶且无在途请求时排队者永久挂起；改为「等待者入队后再排定定时泵送」，已在自测中验证恢复。
- ⬜ 阶段 2（UI 增强包）：宣纸质感 / 动效进阶 / 暗色模式 / 胶囊 chip 筛选。
- ⬜ 阶段 3（P1 扩展）：搜索（诗题/作者，API+本地）、抽卡统计（次数 + 朝代分布）、「重试恢复」控件（阶段1已含基础版）。
- ⬜ 阶段 4（可选）：PWA / canvas 分享图 / 全库浏览 / 收藏导出 / 每日一签。

## 一、现状回顾
- 已修复（v2.0.1）：随机请求降为每轮 3 个 + 160ms 间隔；429 读 `Retry-After` 退避；新增本地兜底库 `src/poems.local.json`（70 首，ID 与 API 对齐），限流/离线时自动顶上。
- 阶段 1 已落地：主动令牌桶 + 熔断器 + 显式失败提示 + 降级横幅 + 骨架屏（详见 README「关键策略」）。
- 待强化（后续阶段）：
  1. UI 国风质感、动效、暗色模式、胶囊 chip 筛选（阶段 2）。
  2. 搜索、抽卡统计等扩展功能（阶段 3）。

## 二、最佳实践（已调研，附来源）
- **主动限流（令牌桶/漏桶）** 比被动重试更优——在触发 429 之前就把速率压在阈值以下。（apipark《Handling Rate Limited》、ergonode《Best Practices》）
- **指数退避 + 全抖动（full jitter）**：`wait = random(0, base·2ⁿ)`，避免大量客户端同步重试形成「惊群」。（Postman《HTTP 429》、apipark）
- **优先尊重 `Retry-After` 头**——服务端最清楚自己的限流窗口。
- **熔断器（Circuit Breaker）**：连续失败达阈值→开路暂停请求一段时间→半开探测恢复。（apipark、ergonode）
- **离线优先（offline-first）**：App Shell 缓存 + 网络优先/缓存兜底 + 本地数据兜底。（Chrome Workbox、PWA 文献）

## 三、架构方案
### 新增/重构模块
- `src/rate-limit.js`：**令牌桶 + 并发上限**调度器 `RequestScheduler`。
- `src/circuit-breaker.js`：熔断器（CLOSED / OPEN / HALF_OPEN）。
- `src/api.js`：统一 `apiRequest(path)`，组合「调度 → 退避(全抖动) → 熔断 → 失败分类」。
- `main.js`：消费上述；新增状态机 `online | throttled | degraded`；失败**显式**提示；降级横幅。
- `styles.css`：美化 + 降级横幅 + 骨架屏 + 暗色。
- （可选）`sw.js` + `manifest.webmanifest`：PWA 离线。

### 1) 严格控制请求频率（核心）
- **令牌桶**：容量 6，补充速率 3/s（保守，远低于触发 429 的并发量）。
- **并发上限**：`/api/poems/random` 同时最多 2 个在途。
- **单飞（single-flight）**：`/api/types`、`/api/dynasties` 只拉一次并缓存。
- **退避（全抖动）**：base 300ms，×2ⁿ，上限 8s，有 `Retry-After` 用之。
- **围栏**：熔断开路期间，后台补池暂停，不再发请求。

### 2) 失败处理（按你的要求：显式提示 + 请稍后重试 / 降级本地）
状态机：
- **ONLINE**：正常。
- **THROTTLED（瞬时 429/5xx）**：非阻塞轻提示「诗泉接口繁忙，正在退避重试…」，自动退避后重试；**不降级**。
- **DEGRADED（重试耗尽 / 熔断开路 / 离线）**：
  - 顶部常驻横幅：「⚠ 诗泉接口暂不可用（限流/离线），已切换本地诗词库（70 首）· [重试恢复]」。
  - 抽卡仍可用（本地库），筛选照常工作。
  - 「[重试恢复]」按钮：触发半开探测，成功则回 ONLINE 并清横幅。
  - 文案区分：`navigator.onLine === false` → 「网络已断开」；429 → 「接口限流」。

### 3) UI 美化
- 宣纸质感背景（CSS 渐变 + SVG 噪点）、水墨风 hero。
- 卡片：朱砂印章进阶、飞白描边、翻牌/入场动效、收藏星 pop 动画。
- **骨架屏 shimmer**（替代「正在寻诗」纯文本）。
- 筛选改为胶囊 chip。
- **降级横幅**组件（醒目但不遮挡操作）。
- **暗色模式**（跟随系统 + 手动切换）。
- Toast 重做，更克制。

### 4) 扩展功能（候选，分优先级）
- **P1（建议必做，契合诉求）**
  1. 搜索：按诗题/作者（`/api/search` + 本地库匹配）。
  2. 降级态「重试恢复」控件（见上）。
  3. 抽卡统计：次数 + 朝代分布小图（纯前端）。
- **P2（体验增强）**
  4. 键盘快捷键：空格 = 抽卡。
  5. 分享卡片：canvas 导出当前诗为图片（下载/分享）。
  6. 暗色模式。
  7. **PWA**：`manifest` + `service worker`（真正离线，App Shell 缓存）。
- **P3（可选）**
  8. 全库浏览/翻页（`/api/poems`）。
  9. 收藏夹导出 JSON。
  10. 每日一签 + 本地通知。

## 四、实施阶段（建议）
- **阶段 1（核心诉求，必做）**：令牌桶 + 熔断 + 显式失败/降级横幅 + 骨架屏。
- **阶段 2（UI）**：宣纸质感 / 动效 / 暗色 / chip 筛选。
- **阶段 3（P1 扩展）**：搜索 / 统计 / 重试恢复。
- **阶段 4（可选）**：P2 / P3（PWA / 分享 / 浏览 / 导出）。

## 五、风险与注意
- **PWA 仅在 http(s) 下生效**：双击 `file://` 打开时 Service Worker 不注册。需以本地服务（如 `npx http-server`）或部署后使用。若坚持「双击即玩」，PWA 降级为「纯本地 JSON 兜底」（不依赖 SW）。
- 令牌桶参数（6/3s）为保守估值；若仍有 429，可据响应头 `X-RateLimit-*` 自适应下调。
